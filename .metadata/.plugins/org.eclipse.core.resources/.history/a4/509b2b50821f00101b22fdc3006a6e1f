package com.example.demo;

import com.example.demo.Lunch_MENU;
import org.springframework.data.jpa.repository.JpaRepository;

public interface LunchMenuRepository extends JpaRepository<Lunch_MENU, Long> {
}
